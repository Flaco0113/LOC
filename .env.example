# Optional email delivery. Disabled unless explicitly enabled.
LOC_EMAIL_MODE=disabled
# Use preview for filesystem-only test messages, or resend for actual delivery.
RESEND_API_KEY=
LOC_EMAIL_FROM=
LOC_PUBLIC_URL=http://localhost:4173
# The app remains bound to loopback; email links work on the host computer.
